<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    [ARQUITETURA] Tema da Splash Screen.
    Este tema é referenciado pelo AndroidManifest e é necessário para a splash nativa.
    A estilização é mínima, pois o controle visual está no Compose.
    -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.App.Main</item>
    </style>

    <!--
    [ARQUITETURA] Tema Principal do App.
    Referenciado pelo AndroidManifest. Este é o tema base após a splash.
    Ele é intencionalmente mínimo para não conflitar com o `SystemTheme` Composable.
    -->
    <style name="Theme.App.Main" parent="android:Theme.Material.Light.NoActionBar">
        <!-- A estilização das barras de sistema é 100% controlada via Compose (`SystemTheme.kt`). -->
    </style>
</resources>
